<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/homeStyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <title>Trang Chủ</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: white;
            text-decoration-color: black;
        }

        /* Header */
        header {
            background-color: white;
            padding: 0px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 100px;
            text-decoration-color: black;
            
        }

        .logo {
            width: 40px;
            height: 40px;
            background-color: black;
            border-radius: 50%;
            margin-left: 20px;
        }

        .nav-links {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .nav-links a {
            text-decoration: none;
            color: black;
            font-weight: bold;
        }

        .right-section {
            margin-right: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .search-btn {
            background-color: black;
            color: white;
            border: none;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
        }

        /* Layout */
        .container {
            display: flex;
            padding: 20px;

        }

        .main-content {
            flex: 3;
            padding-right: 20px;
            margin-left: 30px;
            background-color: white;
            padding: 20px;
        }

        .sidebar {
            flex: 0.5;
            background-color: white;
            
        }

        .post {
            background-color: white;
            padding: 20px;
            margin-bottom: 20px;
            margin-left: 30px;
            border-top: 1px solid rgba(0, 0, 0, 0.3);
            padding-bottom: 10px;
        }
    </style>
</head>

<body>
    {{> header}} <!-- Import phần header -->
    <div class="container" >
        <div class="sidebar col-md-4 d-flex" style="margin-left: -120px;background-color: white;max-height: fit-content;">
            <img src="/static/img/banner.png" style="width: 380px;height: max-content;margin-left: 30px;margin-right: 15px;margin-top: 20px;">
        </div>
        <div class="main-content">
            {{{body}}} <!-- Nội dung từng trang -->
        </div>

    </div>
</body>

</html>